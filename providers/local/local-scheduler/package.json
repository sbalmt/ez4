{
  "name": "@ez4/local-scheduler",
  "description": "EZ4: Local scheduler emulator",
  "version": "0.38.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "ez4-local"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/local/local-scheduler"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    },
    "./test": {
      "types": "./dist/test.d.ts",
      "require": "./dist/test.cjs",
      "import": "./dist/test.mjs"
    }
  },
  "workspaces": [
    "foundation/*",
    "providers/local/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/local-scheduler",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/common": "^0.38.0",
    "@ez4/local-common": "^0.38.0",
    "@ez4/project": "^0.38.0",
    "@ez4/scheduler": "^0.38.0",
    "@ez4/utils": "^0.38.0",
    "cron-parser": "^5.3.0"
  }
}
