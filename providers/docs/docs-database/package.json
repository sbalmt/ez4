{
  "name": "@ez4/docs-database",
  "description": "EZ4: Database documentation generator",
  "version": "0.33.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "ez4 docs"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/docs/docs-database"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    }
  },
  "workspaces": [
    "foundation/*",
    "providers/docs/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/docs-database",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/project": "^0.33.0",
    "@ez4/database": "^0.33.0",
    "@ez4/schema": "^0.33.0",
    "@ez4/utils": "^0.33.0"
  }
}