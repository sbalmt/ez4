{
  "name": "@ez4/aws-gateway",
  "description": "EZ4: Self-managed AWS API gateway provider",
  "version": "0.27.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/aws/aws-gateway"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    }
  },
  "workspaces": [
    "foundation/*",
    "contracts/*",
    "providers/aws/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && ez4 test -e ../../../local.env",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/aws-gateway",
    "live:publish": "npm run build && npm publish --access public"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewayv2": "^3.758.0",
    "@ez4/aws-common": "^0.27.0",
    "@ez4/aws-function": "^0.27.0",
    "@ez4/aws-identity": "^0.27.0",
    "@ez4/aws-logs": "^0.27.0",
    "@ez4/gateway": "^0.27.0",
    "@ez4/project": "^0.27.0",
    "@ez4/schema": "^0.27.0",
    "@ez4/stateful": "^0.27.0",
    "@ez4/transform": "^0.27.0",
    "@ez4/utils": "^0.27.0",
    "@ez4/validator": "^0.27.0"
  }
}