{
  "name": "@ez4/aws-cloudfront",
  "description": "EZ4: Self-managed AWS CloudFront provider",
  "version": "0.55.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "cloudfront",
    "cdn",
    "aws"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/aws/aws-cloudfront"
  },
  "engines": {
    "node": ">=24.21"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "default": "./dist/main.mjs"
    }
  },
  "sideEffects": [
    "./dist/main.mjs"
  ],
  "workspaces": [
    "foundation/*",
    "contracts/*",
    "providers/aws/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -rf dist/* *.tsbuildinfo .eslintcache",
    "build": "tsc -p tsconfig.pack.json && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json --noEmit && ez4 test -e ../../../local.env",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/aws-cloudfront",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.1127.0",
    "@ez4/aws-bucket": "^0.55.0",
    "@ez4/aws-certificate": "^0.55.0",
    "@ez4/aws-common": "^0.55.0",
    "@ez4/aws-identity": "^0.55.0",
    "@ez4/distribution": "^0.55.0",
    "@ez4/logger": "^0.55.0",
    "@ez4/project": "^0.55.0",
    "@ez4/state": "^0.55.0",
    "@ez4/utils": "^0.55.0",
    "esbuild": "^0.28.1"
  }
}
