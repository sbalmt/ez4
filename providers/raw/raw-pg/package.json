{
  "name": "@ez4/raw-pg",
  "description": "EZ4: External Postgres provider (connect to any Postgres via TCP, e.g. Supabase, Neon, RDS direct)",
  "version": "0.55.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "postgres",
    "pg",
    "supabase",
    "neon"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "providers/raw/raw-pg"
  },
  "engines": {
    "node": ">=24.21"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "default": "./dist/main.mjs"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./dist/client.mjs"
    }
  },
  "sideEffects": [
    "./dist/main.mjs"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -rf dist/* *.tsbuildinfo .eslintcache",
    "build": "tsc -p tsconfig.pack.json && node tools/bundler.mjs",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../../.registry/@ez4/raw-pg",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/aws-common": "^0.55.0",
    "@ez4/common": "^0.55.0",
    "@ez4/database": "^0.55.0",
    "@ez4/pgclient": "^0.55.0",
    "@ez4/pgmigration": "^0.55.0",
    "@ez4/project": "^0.55.0",
    "@ez4/state": "^0.55.0",
    "@ez4/utils": "^0.55.0"
  }
}
