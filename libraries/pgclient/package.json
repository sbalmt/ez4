{
  "name": "@ez4/pgclient",
  "description": "EZ4: A PostgreSQL client",
  "version": "0.31.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "libraries/pgclient"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    },
    "./library": {
      "types": "./dist/library.d.ts",
      "require": "./dist/library.cjs",
      "import": "./dist/library.mjs"
    }
  },
  "sideEffects": false,
  "workspaces": [
    "contracts/*",
    "libraries/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../.registry/@ez4/pgclient",
    "live:publish": "npm run build && npm publish --access public"
  },
  "devDependencies": {
    "@ez4/pgmigration": "^0.31.0",
    "@types/pg": "^8.15.5"
  },
  "dependencies": {
    "@ez4/common": "^0.31.0",
    "@ez4/database": "^0.31.0",
    "@ez4/pgsql": "^0.31.0",
    "@ez4/project": "^0.31.0",
    "@ez4/schema": "^0.31.0",
    "@ez4/transform": "^0.31.0",
    "@ez4/utils": "^0.31.0",
    "@ez4/validator": "^0.31.0",
    "pg": "^8.16.3"
  }
}