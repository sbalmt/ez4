{
  "name": "@ez4/hello-aws-sqs",
  "description": "Example of a service using AWS SQS.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "tsc -p tsconfig.json && node --env-file ../../local.env tools/deploy.mjs",
    "destroy": "node --env-file ../../local.env tools/destroy.mjs"
  },
  "dependencies": {
    "@ez4/aws-queue": "^0.1.0",
    "@ez4/queue": "^0.1.0",
    "@ez4/project": "^0.1.0"
  }
}