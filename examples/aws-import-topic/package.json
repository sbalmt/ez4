{
  "name": "aws-import-topic",
  "description": "Example of a service using an imported AWS Topic (SNS).",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc -p tsconfig.json && ez4 deploy -e local.env -p ez4.project.js",
    "destroy": "ez4 destroy -e local.env -p ez4.project.js",
    "serve": "ez4 serve -e local.env -p ez4.project.js"
  },
  "dependencies": {
    "hello-aws-topic": "^0.0.0",
    "@ez4/aws-topic": "^0.26.0",
    "@ez4/aws-queue": "^0.26.0",
    "@ez4/common": "^0.26.0",
    "@ez4/local-topic": "^0.26.0",
    "@ez4/local-queue": "^0.26.0",
    "@ez4/topic": "^0.26.0",
    "@ez4/queue": "^0.26.0",
    "@ez4/project": "^0.26.0"
  }
}