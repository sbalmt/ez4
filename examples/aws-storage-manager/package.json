{
  "name": "aws-storage-manager",
  "description": "Example of a service using AWS S3.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force",
    "generate:topology": "ez4 generate -e local.env -- topology:graph docs/",
    "generate:oas": "ez4 generate -e local.env -- gateway:oas docs/",
    "serve": "ez4 serve -e local.env",
    "output": "ez4 output -e local.env",
    "test": "ez4 test -e local.env"
  },
  "dependencies": {
    "@ez4/aws-bucket": "^0.39.0",
    "@ez4/aws-dynamodb": "^0.39.0",
    "@ez4/aws-gateway": "^0.39.0",
    "@ez4/common": "^0.39.0",
    "@ez4/database": "^0.39.0",
    "@ez4/docs-gateway": "^0.39.0",
    "@ez4/docs-topology": "^0.39.0",
    "@ez4/gateway": "^0.39.0",
    "@ez4/local-database": "^0.39.0",
    "@ez4/local-gateway": "^0.39.0",
    "@ez4/local-storage": "^0.39.0",
    "@ez4/project": "^0.39.0",
    "@ez4/schema": "^0.39.0",
    "@ez4/storage": "^0.39.0"
  }
}