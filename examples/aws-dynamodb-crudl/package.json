{
  "name": "aws-dynamodb-crudl",
  "description": "Example of a service using AWS DynamoDB for CRUDL.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force",
    "serve": "ez4 serve -e local.env"
  },
  "dependencies": {
    "@ez4/aws-dynamodb": "^0.32.0",
    "@ez4/aws-gateway": "^0.32.0",
    "@ez4/common": "^0.32.0",
    "@ez4/database": "^0.32.0",
    "@ez4/gateway": "^0.32.0",
    "@ez4/local-database": "^0.32.0",
    "@ez4/local-gateway": "^0.32.0",
    "@ez4/project": "^0.32.0",
    "@ez4/schema": "^0.32.0"
  }
}