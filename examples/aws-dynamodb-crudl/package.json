{
  "name": "aws-dynamodb-crudl",
  "description": "Example of a service using AWS DynamoDB for CRUDL.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "seed": "tsc && ez4 run -e local.env -- ./src/seed.ts",
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force",
    "generate:oas": "ez4 generate -e local.env -- gateway:oas docs/",
    "serve": "ez4 serve -e local.env"
  },
  "dependencies": {
    "@ez4/aws-dynamodb": "^0.36.0",
    "@ez4/aws-gateway": "^0.36.0",
    "@ez4/common": "^0.36.0",
    "@ez4/database": "^0.36.0",
    "@ez4/docs-gateway": "^0.36.0",
    "@ez4/gateway": "^0.36.0",
    "@ez4/local-database": "^0.36.0",
    "@ez4/local-gateway": "^0.36.0",
    "@ez4/project": "^0.36.0",
    "@ez4/schema": "^0.36.0"
  }
}