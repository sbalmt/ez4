{
  "name": "aws-import-gateway",
  "description": "Example of a service using an imported AWS API Gateway.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env",
    "serve": "ez4 serve -e local.env",
    "output": "ez4 output -e local.env"
  },
  "dependencies": {
    "hello-aws-gateway": "^0.0.0",
    "@ez4/aws-gateway": "^0.40.0",
    "@ez4/local-gateway": "^0.40.0",
    "@ez4/gateway": "^0.41.1",
    "@ez4/project": "^0.40.0"
  }
}