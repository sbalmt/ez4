{
  "name": "aws-dynamodb-streams",
  "description": "Example of a service using AWS DynamoDB streams.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc -p tsconfig.json && ez4 deploy -e local.env -p ez4.project.js",
    "destroy": "ez4 destroy -e local.env -p ez4.project.js --force"
  },
  "dependencies": {
    "@ez4/aws-dynamodb": "^0.26.0",
    "@ez4/common": "^0.26.0",
    "@ez4/database": "^0.26.0",
    "@ez4/project": "^0.26.0"
  }
}