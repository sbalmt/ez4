{
  "name": "aws-dynamodb-streams",
  "description": "Example of a service using AWS DynamoDB streams.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force"
  },
  "dependencies": {
    "@ez4/aws-dynamodb": "^0.37.0",
    "@ez4/common": "^0.37.0",
    "@ez4/database": "^0.37.0",
    "@ez4/project": "^0.37.0"
  }
}