{
  "name": "aws-schedule-manager",
  "description": "Example of a service using AWS EventBridge scheduler.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force",
    "serve": "ez4 serve -e local.env"
  },
  "dependencies": {
    "@ez4/aws-dynamodb": "^0.30.0",
    "@ez4/aws-gateway": "^0.30.0",
    "@ez4/aws-scheduler": "^0.30.0",
    "@ez4/common": "^0.30.0",
    "@ez4/database": "^0.30.0",
    "@ez4/gateway": "^0.30.0",
    "@ez4/local-database": "^0.30.0",
    "@ez4/local-gateway": "^0.30.0",
    "@ez4/local-scheduler": "^0.30.0",
    "@ez4/project": "^0.30.0",
    "@ez4/scheduler": "^0.30.0",
    "@ez4/schema": "^0.30.0"
  }
}