{
  "name": "hello-aws-aurora",
  "description": "Example of a service using AWS Aurora.",
  "version": "0.0.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.7"
  },
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "deploy": "tsc && ez4 deploy -e local.env",
    "destroy": "ez4 destroy -e local.env --force",
    "generate:erd": "ez4 generate -- database:erd docs/"
  },
  "dependencies": {
    "@ez4/aws-aurora": "^0.43.0",
    "@ez4/docs-database": "^0.43.0",
    "@ez4/database": "^0.43.0",
    "@ez4/project": "^0.43.0"
  }
}