# Auto-generated Open API specification, any manual modifications will be lost during regeneration.
openapi: 3.1.0
info:
  title: Hello AWS API
  version: 1.0.0

paths:
  /post-route:
    post:
      operationId: postRoute
      summary: "Handler for `post` requests."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/postHandler'
      responses:
        201:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/postHandler'
  /patch-route/{id}:
    patch:
      operationId: patchRoute
      summary: "Handler for `patch` requests."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/patchHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/patchHandler'
  /put-route/{id}:
    put:
      operationId: putRoute
      summary: "Handler for `put` requests."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/putHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/putHandler'
  /get-route/{id}:
    get:
      operationId: getRoute
      summary: "Handler for `get` requests."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
        - name: foo
          in: query
          required: false
          schema:
            type: array
            description: "Example of query string array."
            items:
              type: string
        - name: bar
          in: query
          required: false
          schema:
            type: boolean
            description: "Example of query string boolean."
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/getHandler'
  /delete-route/{id}:
    delete:
      operationId: deleteRoute
      summary: "Handler for `delete` requests."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the delete parameters."
            format: uuid
      responses:
        204:
          description: "Successful response."
  /raw-route/{proxy+}:
    any:
      operationId: rawRoute
      summary: "Handler for `raw` requests."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/rawHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/rawHandler'

components:
  requestSchemes:
    postHandler:
      type: object
      description: "Expected HTTP body payload."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    patchHandler:
      type: object
      description: "Expected HTTP body payload."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    putHandler:
      type: object
      description: "Expected HTTP body payload."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    rawHandler:
      type: string
      description: "Expected HTTP body payload."

  responseSchemes:
    postHandler:
      type: object
      description: "HTTP body payload."
      additionalProperties: false
      properties:
        id:
          type: string
          description: "Example of `id` in the response."
      required:
        - id
    patchHandler:
      type: object
      description: "HTTP body payload."
      additionalProperties: false
      properties:
        message:
          type: string
          description: "Example of `message` in the response."
      required:
        - message
    putHandler:
      type: object
      description: "HTTP body payload."
      additionalProperties: false
      properties:
        message:
          type: string
          description: "Example of `message` in the response."
      required:
        - message
    getHandler:
      type: object
      description: "HTTP body payload."
      additionalProperties: false
      properties:
        foo_bar:
          type: string
          description: "Example of `string` in the response."
        baz_qux:
          type: string
          description: "Example of base64-encoded response."
          format: byte
      required:
        - foo_bar
        - baz_qux
    rawHandler:
      type: string
      description: "HTTP body payload."
