# Auto-generated Open API specification, any manual modifications will be lost during regeneration.
openapi: 3.1.0
info:
  title: Hello AWS API
  version: 1.0.0

paths:
  /post-route:
    post:
      operationId: postRoute
      summary: "Post endpoint."
      description: "Example of a `POST` endpoint."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/postHandler'
      responses:
        201:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/postHandler'
  /patch-route/{id}:
    patch:
      operationId: patchRoute
      summary: "Patch endpoint."
      description: "Example of a `PATCH` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/patchHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/patchHandler'
  /put-route/{id}:
    put:
      operationId: putRoute
      summary: "Put endpoint."
      description: "Example of a `PUT` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/putHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/putHandler'
  /get-route/{id}:
    get:
      operationId: getRoute
      summary: "Get endpoint."
      description: "Example of a `GET` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the path parameters."
            format: uuid
        - name: foo
          in: query
          required: false
          schema:
            type: array
            description: "Example of query string array."
            items:
              type: string
        - name: bar
          in: query
          required: false
          schema:
            type: boolean
            description: "Example of query string boolean."
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/getHandler'
  /delete-route/{id}:
    delete:
      operationId: deleteRoute
      summary: "Delete endpoint."
      description: "Example of a `DELETE` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: "Example of `id` in the delete parameters."
            format: uuid
      responses:
        204:
          description: "Successful response."
  /raw-route/{proxy+}:
    any:
      operationId: rawRoute
      summary: "Raw endpoint."
      description: "Example of a raw endpoint accepting any HTTP-Verb and content."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/rawHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/rawHandler'
  /get-static-route:
    get:
      operationId: getStaticHandler
      summary: "Get static file."
      description: "Example of a `GET` endpoint for static files."
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/getStaticHandler'

components:
  requestSchemes:
    postHandler:
      type: object
      description: "Typed request body payload.2728- Automatically parsed into the declared types.76- Supports JSON objects and raw string payloads.125- Shape is determined by the declared contract."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    patchHandler:
      type: object
      description: "Typed request body payload.2728- Automatically parsed into the declared types.76- Supports JSON objects and raw string payloads.125- Shape is determined by the declared contract."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    putHandler:
      type: object
      description: "Typed request body payload.2728- Automatically parsed into the declared types.76- Supports JSON objects and raw string payloads.125- Shape is determined by the declared contract."
      additionalProperties: false
      properties:
        foo:
          type: string
          description: "Example of validated `string` coming from the body request."
      required:
        - foo
    rawHandler:
      type: string
      description: "Typed request body payload.2728- Automatically parsed into the declared types.76- Supports JSON objects and raw string payloads.125- Shape is determined by the declared contract."

  responseSchemes:
    postHandler:
      type: object
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
      additionalProperties: false
      properties:
        id:
          type: string
          description: "Example of `id` in the response."
      required:
        - id
    patchHandler:
      type: object
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
      additionalProperties: false
      properties:
        message:
          type: string
          description: "Example of `message` in the response."
      required:
        - message
    putHandler:
      type: object
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
      additionalProperties: false
      properties:
        message:
          type: string
          description: "Example of `message` in the response."
      required:
        - message
    getHandler:
      type: object
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
      additionalProperties: false
      properties:
        foo_bar:
          type: string
          description: "Example of `string` in the response."
        baz_qux:
          type: string
          description: "Example of base64-encoded response."
          format: byte
      required:
        - foo_bar
        - baz_qux
    rawHandler:
      type: string
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
    getStaticHandler:
      type: object
      description: "Typed HTTP response body.2526- Automatically removes fields not matching the contract.84- Shape is determined by the declared contract.132- Supports JSON objects and scalar payloads."
      additionalProperties: false
      properties:
        message:
          type: string
