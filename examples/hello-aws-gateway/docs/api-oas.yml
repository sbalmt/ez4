# Auto-generated Open API specification, any manual modifications will be lost during regeneration.
openapi: 3.1.0
info:
  title: Hello AWS API
  version: 1.0.0

paths:
  /post-route:
    post:
      operationId: postRoute
      summary: "Post endpoint."
      description: "Example of a `POST` endpoint."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/postHandler'
      responses:
        201:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/postHandler'
  /patch-route/{id}:
    patch:
      operationId: patchRoute
      summary: "Patch endpoint."
      description: "Example of a `PATCH` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/patchHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/patchHandler'
  /put-route/{id}:
    put:
      operationId: putRoute
      summary: "Put endpoint."
      description: "Example of a `PUT` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/putHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/putHandler'
  /get-route/{id}:
    get:
      operationId: getRoute
      summary: "Get endpoint."
      description: "Example of a `GET` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: foo
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
        - name: bar
          in: query
          required: false
          schema:
            type: boolean
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/getHandler'
  /delete-route/{id}:
    delete:
      operationId: deleteRoute
      summary: "Delete endpoint."
      description: "Example of a `DELETE` endpoint."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        204:
          description: "Successful response."
  /raw-route/{proxy+}:
    any:
      operationId: rawRoute
      summary: "Raw endpoint."
      description: "Example of a raw endpoint accepting any HTTP-Verb and content."
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/requestSchemes/rawHandler'
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/rawHandler'
  /get-static-route:
    get:
      operationId: getStaticHandler
      summary: "Get static file."
      description: "Example of a `GET` endpoint for static files."
      responses:
        200:
          description: "Successful response."
          content:
            application/json:
              schema:
                $ref: '#/components/responseSchemes/getStaticHandler'

components:
  requestSchemes:
    postHandler:
      type: object
      additionalProperties: false
      properties:
        foo:
          type: string
      required:
        - foo
    patchHandler:
      type: object
      additionalProperties: false
      properties:
        foo:
          type: string
      required:
        - foo
    putHandler:
      type: object
      additionalProperties: false
      properties:
        foo:
          type: string
      required:
        - foo
    rawHandler:
      type: string

  responseSchemes:
    postHandler:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
      required:
        - id
    patchHandler:
      type: object
      additionalProperties: false
      properties:
        message:
          type: string
      required:
        - message
    putHandler:
      type: object
      additionalProperties: false
      properties:
        message:
          type: string
      required:
        - message
    getHandler:
      type: object
      additionalProperties: false
      properties:
        foo_bar:
          type: string
        baz_qux:
          type: string
          format: byte
      required:
        - foo_bar
        - baz_qux
    rawHandler:
      type: string
    getStaticHandler:
      type: object
      additionalProperties: false
      properties:
        message:
          type: string
