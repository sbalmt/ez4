{
  "name": "@ez4/logger",
  "description": "EZ4: Structured and dynamic logging",
  "version": "0.40.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "foundation/logger"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    "types": "./dist/main.d.ts",
    "require": "./dist/main.cjs",
    "import": "./dist/main.mjs"
  },
  "sideEffects": false,
  "workspaces": [
    "foundation/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -rf dist/* *.tsbuildinfo .eslintcache",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../.registry/@ez4/logger",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/utils": "^0.40.0"
  }
}