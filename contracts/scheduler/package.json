{
  "name": "@ez4/scheduler",
  "description": "EZ4: Components to build scheduler services",
  "version": "0.27.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "contracts/scheduler"
  },
  "engines": {
    "node": ">=22.7"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "require": "./dist/main.cjs",
      "import": "./dist/main.mjs"
    },
    "./library": {
      "types": "./dist/library.d.ts",
      "require": "./dist/library.cjs",
      "import": "./dist/library.mjs"
    },
    "./utils": {
      "types": "./dist/utils.d.ts",
      "require": "./dist/utils.cjs",
      "import": "./dist/utils.mjs"
    }
  },
  "workspaces": [
    "foundation/*",
    "contracts/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -f *.tsbuildinfo && rm -rf dist/*",
    "build": "tsc && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../.registry/@ez4/scheduler",
    "live:publish": "npm run build && npm publish --access public"
  },
  "dependencies": {
    "@ez4/common": "^0.27.0",
    "@ez4/project": "^0.27.0",
    "@ez4/reflection": "^0.27.0",
    "@ez4/schema": "^0.27.0",
    "@ez4/transform": "^0.27.0",
    "@ez4/utils": "^0.27.0",
    "@ez4/validator": "^0.27.0"
  }
}