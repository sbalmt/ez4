{
  "name": "@ez4/queue",
  "description": "EZ4: Components to build queue services",
  "version": "0.55.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "queue",
    "contract"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "contracts/queue"
  },
  "engines": {
    "node": ">=24.21"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "default": "./dist/main.mjs"
    },
    "./library": {
      "types": "./dist/library.d.ts",
      "default": "./dist/library.mjs"
    },
    "./utils": {
      "types": "./dist/utils.d.ts",
      "default": "./dist/utils.mjs"
    }
  },
  "sideEffects": [
    "./dist/library.mjs"
  ],
  "workspaces": [
    "foundation/*",
    "contracts/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -rf dist/* *.tsbuildinfo .eslintcache",
    "build": "tsc -p tsconfig.pack.json && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json --noEmit && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../.registry/@ez4/queue",
    "live:publish": "npm run build && npm publish --access public"
  },
  "devDependencies": {
    "@ez4/validation": "^0.55.0"
  },
  "dependencies": {
    "@ez4/common": "^0.55.0",
    "@ez4/project": "^0.55.0",
    "@ez4/reflection": "^0.55.0",
    "@ez4/schema": "^0.55.0",
    "@ez4/transform": "^0.55.0",
    "@ez4/utils": "^0.55.0",
    "@ez4/validator": "^0.55.0"
  }
}
