{
  "name": "@ez4/validation",
  "description": "EZ4: Components to build validation services",
  "version": "0.55.0",
  "author": "Silas B.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "ez4",
    "validation",
    "contract"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbalmt/ez4.git",
    "directory": "contracts/validation"
  },
  "engines": {
    "node": ">=24.21"
  },
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "default": "./dist/main.mjs"
    },
    "./library": {
      "types": "./dist/library.d.ts",
      "default": "./dist/library.mjs"
    },
    "./test": {
      "types": "./dist/test.d.ts",
      "default": "./dist/test.mjs"
    }
  },
  "sideEffects": [
    "./dist/library.mjs"
  ],
  "workspaces": [
    "foundation/*",
    "contracts/*"
  ],
  "scripts": {
    "lint": "eslint --cache",
    "clean": "rm -rf dist/* *.tsbuildinfo .eslintcache",
    "build": "tsc -p tsconfig.pack.json && node tools/bundler.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json --noEmit && ez4 test",
    "local:publish": "npm run build && npm run clean:registry && npm publish --registry http://localhost:4873",
    "clean:registry": "rm -rf ../../.registry/@ez4/validation",
    "live:publish": "npm run build && npm publish --access public"
  },
  "devDependencies": {
    "@ez4/validation": "^0.55.0"
  },
  "dependencies": {
    "@ez4/common": "^0.55.0",
    "@ez4/project": "^0.55.0",
    "@ez4/reflection": "^0.55.0",
    "@ez4/schema": "^0.55.0",
    "@ez4/utils": "^0.55.0",
    "@ez4/validator": "^0.55.0"
  }
}
